// Types
import type { Family, Finish, Frame, Type } from "./frames.types";

type FramesProps = {
  families: Family[];
  finishes: Finish[];
  frames: Frame[];
  types: Type[];
};

export type { FramesProps };
